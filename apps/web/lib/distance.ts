// Re-export distance utilities from shared package
// This maintains backward compatibility with existing imports
export {
  kmToMiles,
  milesToKm,
  formatMiles,
  formatKm,
} from '@chaski/shared-utils'
